import { classes }   from 'Styles/common'
const { cardList } = classes

export default (theme) => ({
  cardList,
  container: {
    alignItems: 'center',
    display: 'flex',
    flexDirection: 'column'
  }
})
